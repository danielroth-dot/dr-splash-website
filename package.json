{
  "name": "dr-splash-website",
  "version": "1.0.0",
  "private": true,
  "description": "Dr. Splash Marketing Website",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 execute dr-splash-db --local --file=./schema.sql",
    "db:migrate": "wrangler d1 execute dr-splash-db --remote --file=./schema.sql",
    "cf-typegen": "wrangler types"
  },
  "dependencies": {
    "hono": "^4.9.6"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250906.0",
    "typescript": "^5.9.2",
    "wrangler": "^4.131.0"
  }
}
